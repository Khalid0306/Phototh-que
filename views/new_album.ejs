<!DOCTYPE html>
<html lang="fr">
<head>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/nav') %>

    <div class="container">
        <div class="row mt-4">
            <div class="col">
                <h1>Nouvel Album</h1>
            </div>
        </div>
        <div class="col">
            <form action="/albums/create" method="POST">
                <div class="form-group">
                    <label>Titre</label>
                    <input class="form-control" type="text" name="albumTitle" placeholder="Titre de votre album">
                </div>

                <% if (errors.length) { %>
                    <div class="alert alert-danger mt-4 mb-2" role="alert">
                        <%= errors[0] %>
                    </div>
                <% } %>
                
                <button type="submit" class="btn btn-primary mt-4 ">
                   Créer 
                </button>
            </form>
        </div>
    </div>

    <%- include('partials/scripts') %>
</body>
</html>